<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="HeaderTextStyle">
        <item name="android:layout_height">@dimen/headerHeight</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textColor">@color/secondary_text</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textSize">@dimen/headerText</item>
        <item name="android:layout_margin">@dimen/generalMargin</item>
    </style>

    <style name="CheckBoxStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingLeft">@dimen/checkBoxStartPadding</item>
        <item name="android:paddingRight">@dimen/checkBoxEndPadding</item>
        <item name="android:textSize">@dimen/normalText</item>
    </style>

    <style name="IncrementDecrementButtonStyle">
        <item name="android:layout_width">@dimen/buttonWidth</item>
        <item name="android:layout_height">@dimen/buttonHeight</item>
        <item name="android:layout_marginBottom">@dimen/generalMargin</item>
        <item name="android:layout_marginTop">@dimen/generalMargin</item>
    </style>

</resources>
